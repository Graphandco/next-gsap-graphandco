import Logo from './Logo';
import Nav from './Nav';

const Header = () => {
    return (
        <header className="container">
            <Logo />
            <Nav />
        </header>
    );
};

export default Header;
